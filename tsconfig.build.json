{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src",   // Only compile src files
    "outDir": "./dist"    // Output compiled files into dist
  },
  "exclude": [
    "node_modules",
    "test", "dist",
    "**/*spec.ts"
  ]
}
